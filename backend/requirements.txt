# Core training pipeline (Module 03 - recommendation)
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
joblib>=1.3

# Optional: used when installed, a bundled fallback is used otherwise
networkx>=3.1

# Optional: only needed if you serve the module through Flask instead of
# reading the published JSON directly from the React app
Flask>=3.0
flask-cors>=4.0
python-dotenv>=1.0
